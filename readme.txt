Installation procedure

This project assumes that python version 3 is installed on the host computer. All testing was done on Linux (Ubuntu).

1. Install requirements with:

pip install -r requirements.txt

2. Run the crawler:

python crawler.py

3. Run the indexer:

python indexer.py

4. Run the webapp:

flask run

5. Navigate to the webapp (localhost:5000) to interact with the search engine